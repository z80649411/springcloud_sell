eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
#        配置微服务配置中心名字
        service-id: CONFIG
      profile: dev


  jpa:
    show-sql: true

server:
  port: 9080