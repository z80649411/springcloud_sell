spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dengrenbocq/spring-cloud-config-repo.git
          username: dengrenbo@outlook.com
          password: git密码
          basedir: /Users/admin/code/myProjects/java/imooc/SpringCloud_Sell/config/basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      expose: "*"